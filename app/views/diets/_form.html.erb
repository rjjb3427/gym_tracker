<%= simple_form_for [@profile,@diet], :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :title%>
  <%= f.input :active %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                profile_diets_path, :class => 'btn' %>
  </div>
<% end %>